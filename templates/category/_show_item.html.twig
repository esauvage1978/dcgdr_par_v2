    <a href="{{ path('category_show', {'id': category.id}) }}"  class="btn btn-info m-1">

        {% if category.enable == false   %}
            <span class="text-danger" ><i class="fas fa-eye-slash"></i></span>
        {%  endif %}

        {{ category.name }}

    </a>
